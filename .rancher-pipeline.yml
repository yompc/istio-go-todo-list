stages:
- name: 打包
  steps:
  - runScriptConfig:
      image: golang:latest
      shellScript: |-
        cp -r . $GOPATH/src/yom535.coding.net/todo-consumer
        cd $GOPATH/src/yom535.coding.net/todo-consumer
        go build .
timeout: 60
notification: {}
